# LER-1269 — Fixture-debt settlement: decide and execute before any baseline

_UC-09 PDF Generation & Formatting · epic: `docs/usecases/uc-09/epic.md` · PRD: `docs/usecases/uc-09/prd.md`_

_Client ticket **LER-1269** (Story, To Do) · delivering ticket: **itself — delivered on `mgs/ler-1269-fixtures`** · build order: story 01 of 26, stage 0 — nothing baseline-shaped may precede it._

## User Story
As a project, we want the dev.db fixture-pollution decision made and executed before UC-09 captures any reference render, so that a fixture artefact ("Daniel FOSTER (edited)", "not-a-urn") is never baked into an expected image and later misread as a rendering regression.

## Acceptance Criteria
- Given LER-1269's recorded inventory, when UC-09 build starts, then a written decision exists in this pack's open-questions (Q2) choosing one strategy: per-suite dedicated seeded cases, a reset step for case-linked drafts, or pinning tests to the pollution.
- Given the chosen strategy, when it lands, then UC-08's Sofia-Renard workaround is either retired or promoted to the documented pattern — no unexplained special-casing survives.
- Given the user's 27 real drafts, when the strategy executes, then they are byte-identical afterwards unless the decision explicitly says otherwise.
- Given the strategy has landed, when LER-1156 captures reference renders, then the renders derive only from fixtures the strategy sanctioned.

## Notes & Constraints
- The recommendation on the table is per-suite dedicated cases (Sofia-Renard promoted to policy); it is a recommendation, not a default — the ticket exists so this is decided, not drifted into.
- Do not "clean" any baseline value as a side effect (the ticket's own out-of-scope clause).

## Definition of Done
- Decision recorded in `docs/usecases/uc-09/open-questions.md` Q2 with its rationale
- Strategy executed; `npm run test:e2e` green ×2 dev.db + ×1 throwaway; orphan-scan clean
- The 27 real drafts verified byte-identical (or the decision says why not)
